git clone https://github.com/priyadharshika13/email-dev-task.git
cd bulk-email-campaign-system

python -m venv venv
venv\Scripts\activate         # windows
# source venv/bin/activate    # mac/linux

pip install -r requirements.txt

python manage.py migrate
python manage.py createsuperuser

python manage.py runserver
celery -A bulk_email_system worker -l info --pool=solo
celery -A bulk_email_system beat -l info

In settings.py 
add mail and app password
admin_mail 

We can have it call by .env for real time production saas
